import { useState } from 'react'; // นำเข้า useState เพื่อใช้จดจำสิ่งที่ผู้ใช้กำลังพิมพ์

function TaskInput({ onAdd }) {
  // 📝 input: State สำหรับเก็บข้อความในช่องกรอกแบบ Real-time
  const [input, setInput] = useState('');

  // 🚀 ฟังก์ชันจัดการเมื่อมีการกดปุ่ม "Add Task" หรือกด Enter ในฟอร์ม
  const handleSubmit = (e) => {
    e.preventDefault(); // ป้องกันหน้าเว็บรีเฟรช (พฤติกรรมปกติของ HTML Form)
    
    if (!input) return; // ตรวจสอบ: ถ้าช่องว่างอยู่ จะไม่ทำอะไรต่อ (ป้องกันการแอดงานไม่มีชื่อ)
    
    onAdd(input); // 📢 ส่งข้อมูลที่พิมพ์กลับไปยังแอปหลัก (App.js) ผ่านฟังก์ชัน onAdd ที่ได้รับมาเป็น Props
    
    setInput(''); // ✨ เมื่อแอดเสร็จแล้ว ให้ล้างข้อความในช่องกรอกเป็นค่าว่างเหมือนเดิม
  };

  return (
    // ส่วนแสดงผลฟอร์ม
    <form onSubmit={handleSubmit} className="input-group">
      <input 
        /* 🔗 เชื่อมโยงค่าในช่องพิมพ์กับ State 'input' (Controlled Component) */
        value={input} 
        /* ✍️ ทุกครั้งที่พิมพ์ จะอัปเดต State 'input' ทันที */
        onChange={(e) => setInput(e.target.value)} 
        placeholder="เพิ่มงานใหม่..."
      />
      <button type="submit">Add Task</button>
    </form>
  );
}

export default TaskInput;