# 📦 1. ไม่เอา Library ขึ้นไป (มันหนักและโหลดใหม่ได้)
# โฟลเดอร์ node_modules เก็บ Library ทั้งหมดที่ติดตั้ง (เช่น Express, React)
# ซึ่งมีขนาดใหญ่มาก (หลายร้อย MB) ไม่จำเป็นต้องเก็บไว้ใน Git 
# เพราะคนอื่นที่โหลดโปรเจกต์ไปสามารถพิมพ์ 'npm install' เพื่อโหลดใหม่เองได้ตามไฟล์ package.json
node_modules/
server/node_modules/
client/node_modules/

# 🔐 2. ไม่เอาความลับขึ้นไป
# ไฟล์ .env เก็บข้อมูลสำคัญ เช่น รหัสผ่านฐานข้อมูล (MongoDB URI) หรือ API Keys
# การใส่ชื่อไฟล์นี้ไว้ใน .gitignore จะช่วยป้องกันไม่ให้ "ความลับ" หลุดไปสู่สาธารณะบน GitHub
.env
server/.env

# 🏗️ 3. ไม่เอาผลลัพธ์การ Build ขึ้นไป
# โฟลเดอร์ dist (หรือ build) คือไฟล์ที่ถูกแปลง (Compile) แล้วเพื่อนำไปใช้งานจริง
# เราไม่ต้องอัปโหลดขึ้นไป เพราะเมื่อเราสั่ง Deploy บน Vercel 
# ตัวระบบ Vercel จะเป็นคนสั่งรันคำสั่ง Build เพื่อสร้างโฟลเดอร์นี้ขึ้นมาใหม่เองเสมอ
client/dist/